---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---
# WeightLifting01 Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-22

## Active Technologies
- Angular 20 frontend, C# with .NET 10 backend + Angular, Angular Material, RxJS, ASP.NET Core Web API, Entity Framework Core (001-create-lift)
- SQL Server / Azure SQL-compatible relational storage already used by the app; no storage changes planned (001-create-lift)
- Angular 20 frontend, C# with .NET 10 backend + Angular, Angular Material, ASP.NET Core Web API, Entity Framework Core, SQL Server provider (003-rename-lift)
- Azure SQL / SQL Server-compatible relational storage using the existing `Lifts` table (003-rename-lift)
- Angular 20 frontend, C# with .NET 10 backend + Angular, Angular Material, RxJS, ASP.NET Core Web API, Entity Framework Core, SQL Server provider (004-deactivate-lift)
- Azure SQL / SQL Server-compatible relational storage using the existing `Lifts` table and `IsActive` column (004-deactivate-lift)
- Angular 20 frontend, C# .NET 10 backend (unchanged) + Angular Router, Angular Material toolbar/button/nav primitives, existing lifts feature routes (005-default-home-nav)
- Existing SQL-backed backend remains unchanged (no data changes in this feature) (005-default-home-nav)
- Angular 20 (TypeScript) frontend, C# on .NET 10 backend + Angular Router + Angular Material, ASP.NET Core Web API, Entity Framework Core 10 (006-start-blank-workout)
- SQL Server / Azure SQL-compatible relational database via EF Core migrations (006-start-blank-workout)
- SQL Server / Azure SQL-compatible relational database via existing workout persistence (008-home-active-workout)
- Angular 20 (TypeScript) frontend, C# on .NET 10 backend + Angular Material + Angular Router, ASP.NET Core Web API, Entity Framework Core 10 (010-reorder-workout-lifts)

- Angular 20 frontend, C# with .NET 10 backend + Angular, ASP.NET Core Web API, Entity Framework Core, SQL Server provider (001-create-lift)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

# Add commands for Angular 20 frontend, C# with .NET 10 backend

## Code Style

Angular 20 frontend, C# with .NET 10 backend: Follow standard conventions

## Recent Changes
- 010-reorder-workout-lifts: Added Angular 20 (TypeScript) frontend, C# on .NET 10 backend + Angular Material + Angular Router, ASP.NET Core Web API, Entity Framework Core 10
- 009-remove-workout-lift: Added Angular 20 (TypeScript) frontend, C# on .NET 10 backend + Angular Router + Angular Material, ASP.NET Core Web API, Entity Framework Core 10
- 008-home-active-workout: Added Angular 20 (TypeScript) frontend, C# on .NET 10 backend + Angular Router + Angular Material, ASP.NET Core Web API, Entity Framework Core 10


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
